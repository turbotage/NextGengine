#pragma once

#include "../def.h"
#include "../Entities/entity.h"
#include <mutex>

class Renderable
{
private:
	uint8 m_RenderProps;
	
public:

	

	

	Renderable();
	Renderable(uint8 renderProps);
};
